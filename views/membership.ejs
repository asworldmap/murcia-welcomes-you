<div class="container" style="padding-top: 100px;">
    <div style="text-align: center; margin-bottom: 60px;">
        <h1 style="font-size: 3.5rem; color: var(--primary);">Join the Family</h1>
        <p style="font-size: 1.2rem; max-width: 600px; margin: 0 auto;">Everything you need to reset and enjoy January
            in Murcia.</p>
    </div>

    <div class="pricing-card">
        <h2 style="color: var(--naif-orange);">January Full Pass</h2>
        <div class="price">29€</div>
        <ul style="list-style: none; margin: 30px 0; padding: 0; text-align: left;">
            <li style="margin-bottom: 10px;">✨ Access to all 20+ events</li>
            <li style="margin-bottom: 10px;">🍷 1 Welcome Drink included</li>
            <li style="margin-bottom: 10px;">🎒 Exclusive Welcome Pack</li>
            <li style="margin-bottom: 10px;">🤝 VIP Community Access</li>
        </ul>

        <button id="stripe-button" class="btn" style="width: 100%; margin-bottom: 15px;">Pay with Card (Stripe)</button>

        <div id="paypal-button-container"></div>
    </div>
</div>

<script src="https://www.paypal.com/sdk/js?client-id=sb&currency=EUR"></script>
<script>
    // Stripe Logic (Stub)
    document.getElementById('stripe-button').addEventListener('click', function () {
        fetch('/create-checkout-session', {
            method: 'POST',
        })
            .then(response => response.json())
            .then(session => {
                window.location.href = session.url;
            })
            .catch(error => {
                console.error('Error:', error);
            });
    });

    // PayPal Logic
    paypal.Buttons({
        createOrder: function (data, actions) {
            return actions.order.create({
                purchase_units: [{
                    amount: {
                        value: '29.00'
                    }
                }]
            });
        },
        onApprove: function (data, actions) {
            return actions.order.capture().then(function (details) {
                window.location.href = '/success';
            });
        }
    }).render('#paypal-button-container');
</script>